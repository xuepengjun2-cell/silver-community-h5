# Claude 交接说明：同城银发活动 SOP 学习网站

## 项目定位

这是一个面向同城银发社群主理人的活动 SOP 学习网站，包含前台活动展示/学习页和后台管理系统。

核心目标：

- 普通用户通过账号登录后学习活动 SOP。
- 管理员控制用户下载权限、活动上架审核、活动导入。
- 后台维护活动图片、视频参考、方案参考、地区、类型、价格、流程、物料、人力配置、风险控制等内容。

## 运行方式

```bash
cd silver-community-h5
./start.sh
```

访问地址：

- 前台：http://localhost:5174
- 后台：http://localhost:5174/admin

演示账号：

- 管理员：admin / admin123
- 城市主理人：city / city123
- 普通学习用户：member / member123

## 当前功能

- 前台首页：50 个活动方案，按大类分组，支持城市/大类/关键词筛选。
- 活动详情：活动图片、简介、价格、人数、时长、地点、类型、SOP 学习区、执行方案、图片与视频参考。
- SOP 下载：需要登录；普通用户是否可下载由管理员控制。
- 后台活动管理：活动 CRUD、图片上传、状态控制、地区备注、细分类型、视频/参考链接、SOP 下载开关、审核备注。
- 后台导入审核：支持粘贴 JSON 或 `{ activities: [...] }`，导入后默认待审核。
- 后台账号权限：可创建普通学习用户、城市主理人、只读账号、管理员，并控制 SOP 下载权限。

## 主要文件

- `server.js`：Node 原生 HTTP 服务、API、权限、下载、导入审核。
- `public/app.js`：前台活动库、活动详情、登录、SOP 下载。
- `public/styles.css`：前台样式。
- `public/admin.js`：后台管理逻辑。
- `public/admin.css`：后台样式。
- `data/db.json`：当前 50 个活动和账号数据。
- `data/seed-activities.json`：50 个活动的种子数据。

## 界面优化方向

请继续保持干净、高级、克制的视觉方向：

- 色彩：浅底、低饱和、少量橙色品牌点缀，不要大面积高饱和色。
- 图片：人物尽量使用中国 50+ 左右男女模特，画面干净、生活方式感、不过度商业摄影棚感。
- 模块：详情页信息建议继续做模块化、分层展示，避免堆成普通表单。
- 字体：标题要有气质，但内容区不要过大，适合中老年主理人阅读。
- 交互：后台优先清晰高效，不做复杂动效。

## 注意事项

- 不要把 `data/sessions.json` 打包或交付给外部，它只是本地登录会话。
- 当前项目没有依赖复杂框架，方便快速改界面；如需重构为 React/Vue，可保留现有 API 和数据结构。
- 活动数据字段已经包含 `region`、`activityType`、`videos`、`references`、`downloadEnabled`、`reviewNote`。
